spring:
  application:
    name: tool
  cloud:
    config:
      profile: @spring.profiles.active@,application,regular,fastdfs
      label: master
      username: admin
      password: 123456
      fail-fast: true
      discovery:
        service-id: config-server
        enabled: true

eureka:
  client:
    service-url:
      defaultZone: http://admin:123456@${eureka.instance.ip-address:localhost}:${eureka.port:1111}/eureka/

