spring:
  application:
    name: tool
  cloud:
    config:
      profile: config,db,redis,regular,service,fastdfs
      label: master
      username: admin
      password: 123456
      fail-fast: true
      discovery:
        service-id: config-server
        enabled: true

eureka:
  client:
    service-url:
      defaultZone: http://admin:123456@${eureka.host:localhost}:${eureka.port:1111}/eureka/

feign:
  hystrix:
    enabled: true
