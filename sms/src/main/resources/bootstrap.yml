server:
  port: 8001

spring:
  application:
    name: sms
  cloud:
    config:
      profile: config,db,redis,regular,service
      label: master
      username: admin
      password: 123456
      fail-fast: true
      discovery:
        enabled: true
        serviceId: sms-config-server
  http:
    encoding:
      force: true

eureka:
  client:
    service-url:
      defaultZone: http://admin:123456@${eureka.host:123.207.85.155}:${eureka.port:1111}/eureka/

